{% extends 'base.html.twig' %}

{% block title %}Post{% endblock %}

{% block body %}
    <h1>Post</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ post.id }}</td>
            </tr>
            <tr>
                <th>ImageURL</th>
                <td>{{ post.imageURL }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ post.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_post_controller2_index') }}">back to list</a>

    <a href="{{ path('app_post_controller2_edit', {'id': post.id}) }}">edit</a>

    {{ include('post_controller2/_delete_form.html.twig') }}
{% endblock %}
